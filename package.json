{
  "name": "towa-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Towa monorepo — AI agent harness for durable long-term memory recall. See docs/towa-design.md.",
  "type": "module",
  "scripts": {
    "build": "pnpm -r run build",
    "build:w": "pnpm -r --parallel run build:w",
    "typecheck": "pnpm -r run typecheck"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.13.1",
  "devDependencies": {
    "@types/node": "^22.15.32",
    "typescript": "^5.8.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "node-llama-cpp"
    ]
  }
}
