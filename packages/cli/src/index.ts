/**
 * `towa` CLI package — stub.
 *
 * Real CLI product surface lands in a follow-up. Package name `towa` is
 * reserved for the eventual global binary.
 */

export const CLI_PACKAGE_STUB = "towa" as const;
