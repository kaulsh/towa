#!/usr/bin/env node
/**
 * Stub entry for the future `towa` binary.
 * Product behavior is intentionally unimplemented until the CLI+daemon work.
 */

console.log(
  "towa: CLI stub — install works; product commands are not implemented yet.",
);
process.exit(0);
