/**
 * @towa/daemon — stub.
 *
 * Real daemon product surface (CLI+daemon thesis) lands in a follow-up.
 * This package exists so the monorepo workspace/layout is in place.
 */

export const DAEMON_PACKAGE_STUB = "@towa/daemon" as const;
